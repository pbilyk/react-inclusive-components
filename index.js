import * as Components from './lib'

const { Heading, Section, List, Label, Input, LiveArea, Fieldset, Button } = Components;

export { Heading, Section, List, Label, Input, LiveArea, Fieldset, Button };

